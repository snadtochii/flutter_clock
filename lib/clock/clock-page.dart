import 'package:flutter/material.dart';
import 'package:flutter_clock/clock/clock.dart';

class ClockPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: null, body: Clock());
  }
}
